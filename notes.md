
# Random Notes

- Change the scanner to return the '^' character for an exponent operator and change the float token to use it. Now, the exponent is [eE]. That way, the exponent can be used in an expression.